# Dependencias para la aplicación Raspberry Pi Zero 2W

# Control de GPIO
adafruit-blinka>=8.14.0

# Cámara (picamera2 es la nueva librería, más eficiente)
#picamera2>=0.3.12

# I2C y pantalla OLED
#adafruit-circuitpython-ssd1306==1.9.9
# `board` y `busio` son módulos provistos por `adafruit-blinka`, no paquetes pip independientes

# Procesamiento de imágenes
Pillow>=9.5.0

# Servidor web local
Flask>=2.0.0

# GPS por puerto serial
pyserial>=3.5

# Mapas de OpenStreetMap (recomendado)
staticmap>=0.3.0

# Descarga de tiles
requests>=2.28.0

# MQTT - ThingsBoard
paho-mqtt>=1.6.0

# Métricas del sistema (CPU, RAM, disco, temp)
psutil>=5.9.0

